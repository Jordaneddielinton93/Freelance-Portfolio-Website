import PropTypes from "prop-types";

const Primary_ButtonCTA = () => {
  return <button></button>;
};

Primary_ButtonCTA.propTypes = {
  label: PropTypes.string,
  onClick: PropTypes.func,
};

export default Primary_ButtonCTA;
